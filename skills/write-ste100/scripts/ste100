#!/bin/sh
''':'
if ! command -v uv >/dev/null 2>&1; then
  echo "ste100 requires uv. Install it from https://docs.astral.sh/uv/getting-started/installation/ and try again." >&2
  exit 2
fi
exec uv run --locked --script "$0" "$@"
':'''
# /// script
# requires-python = ">=3.11"
# dependencies = [
#   "pdfplumber==0.11.7",
#   "PyYAML==6.0.2",
# ]
# ///

from ste100_cli import main


if __name__ == "__main__":
    raise SystemExit(main())
