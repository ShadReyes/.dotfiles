## Plan Mode

- Make the plan extremely concise. Sacrifice grammar for the sake of concision.
- At the end of each plan, give me a list of unresolved questions to answer, if any.


## RULES
- Never include AI attribution, signatures, or self-identification (e.g., "Generated by Claude") in code comments or commit messages.

## Devcontainer

The Rails backend (`fluid`) runs inside a Docker devcontainer. When working in any Fluid repo that needs the Rails API, use the `devcontainer` skill at `/Volumes/Code/Fluid/.claude/skills/devcontainer/SKILL.md` for setup, commands, and frontend connection instructions. All Rails/Ruby/bundle/rake commands must go through `docker exec`.

## PR Summaries

When creating pull requests, use the template at `~/.claude/templates/pr-summary.md` (GitHub markdown):

1. **What changed** - Brief description of the changes
2. **Why it changed** - Context and motivation
3. **How to test** - Steps to verify the changes
4. **Linear** - Link to the Linear issue

Extract the Linear URL from the branch name or issue lookup.
