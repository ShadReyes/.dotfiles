# Steward instructions

Declared as an OPTIONAL steward instruction source; it composes into every
delegation's trusted prompt (root-first, before the domain agent's own).

- Route writable work by target path; never hand-edit an owned scope.
- Treat `docs/shared/` as the canonical cross-domain reference.
