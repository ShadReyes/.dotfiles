# Web agent instructions

Declared as an OPTIONAL instruction source for the `web` agent, so it composes
into a web delegation's trusted system prompt.

- Keep the application shell in `apps/web/`; reusable components belong to the
  design system under `apps/web/components/` and are out of your write scope.
- Prefer composing existing design-system components over adding new markup.
