# Billing glossary (shared)

Declared as an OPTIONAL context source for the `billing` agent, so it is
snapshot-injected into a billing delegation as untrusted reference material.

- **Line item** — one charge with a description and an integer cent amount.
- **Invoice total** — the sum of all line-item cent amounts.
